import static net.modificationstation.stationapi.gradle.SubprojectHelpers.addModuleDependencies
import static net.modificationstation.stationapi.gradle.SubprojectHelpers.getSubprojectVersion

archives_base_name = "nyalib-block-networks"
version = getSubprojectVersion(project, "1.0.0")

loom {
    accessWidenerPath = file("src/main/resources/nyalib-block-networks.accesswidener")
}

addModuleDependencies(project,
        "nyalib-base"
)